# EduPortal Database

This folder contains a simple SQLite database for the school administration portal.

## Files
- init_db.py: creates and populates the database
- eduportal.db: generated SQLite database

## Usage
Run:

```bash
python database/init_db.py
```

The script will create the database file in the same folder.
